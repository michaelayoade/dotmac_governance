<!-- Generated by dotmac-agent; edit the profile instead. -->
# {{REPOSITORY_NAME}} repository guidance

{{SUMMARY}}

## Authority and routing

Read these checked-in sources before changing behavior:

{{AUTHORITY_SOURCES}}

Report conflicts between authoritative sources instead of guessing. Dynamic
context from MCP or memory aids discovery and never overrides checked-in rules.

## Working agreement

- Work on a non-default branch.
- Keep changes small, focused, and reviewable.
- Never write secret values; record only an approved secret pointer.
- Update the owning checked-in contract when behavior or ownership changes.
- Use hooks and CI for deterministic controls instead of relying on prose.

## Validation

Run the repository-prescribed commands before publication:

{{VALIDATION_COMMANDS}}

## Reporting

State what changed, which validation ran, every failure, and every decision that
still requires a named human. An agent-reported pass is diagnostic context, not
controlled evidence.
