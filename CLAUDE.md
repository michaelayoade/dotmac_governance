@AGENTS.md

# Claude-specific additions

- Treat every `Proposed` ADR and every draft policy as non-normative.
- Use the same checked-in validation commands as Codex; an agent's statement
  that validation passed is not governance evidence.
- Do not copy governance instructions into this file. Shared requirements
  belong in `AGENTS.md`; this file exists only as Claude's import boundary.
