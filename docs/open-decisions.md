# Open decisions

These items require a named human decision. An agent drafted the surrounding
scaffold and deliberately did not infer scope, identity, or evidence claims.

| # | Decision | Why it is blocking | Owner |
| --- | --- | --- | --- |
| 1 | ~~ISMS scope statement for ISO/IEC 27001:2022~~ | Closes if ADR 0002 is approved: replaced by the governed-scope repository list. See decision 9. | Michael |
| 2 | ~~AIMS scope statement for ISO/IEC 42001:2023~~ | Closes if ADR 0002 is approved: replaced by the agent-participation process. | Michael |
| 3 | ~~Named independent evidence verifier(s)~~ | Closes if ADR 0002 is approved: independent verification is certification machinery. Human approval of changes still applies. | Michael |
| 4 | Evidence schema, retention, freshness, and tamper-evident export | Deliberately deferred. Under ADR 0002 this is derived from adopted processes and their information items, so it cannot be designed until the six processes exist. | Control owners |
| 5 | Human/agent identity separation in GitHub and Knowledge | An agent operating through Michael's account is not distinguishable from Michael's own action. Under ADR 0002 this blocks the agent-participation process being verifiable, so it moves ahead of the process definitions. | Michael |
| 6 | Relationship to `dotmac_sub`'s source-of-truth standard | Under ADR 0002 this becomes the architecture-and-design process definition rather than a standalone policy. Confirm that framing. | Michael |
| 7 | Enforced branch protection | Directed 2026-07-26: closed by publishing the repository. ADR 0003 records the decision and its conditions; the item stays open until protected `main` is verified by API. | Michael |
| 8 | Managed Codex/Claude policy rollout and cutover | Partially resolved for one endpoint only: on 2026-07-31 Michael accepted ADR 0005 for `agent:michael-workstation`, designated the local sudo-backed installer, and authorized backup-backed guidance migration. Organization-wide rollout remains gated on decision 5. Adding endpoints, retiring the old Knowledge-bootstrap guidance writer, or broadening policy requires a separate decision after this pilot is verified. | Michael |
| 9 | Governed scope beyond the initial six repositories | `dotmac_academy_app`, `dotmac_voice`, `dotmac_mobile`, `dotmac_vtu`, `dotmac_starter_mt`, `dotmac_data`, and `flutter-xcode-cloud-starter` are active but out of initial scope. In or out, deliberately. | Michael |
| 10 | Canonical location of `dotmac_field` | Referenced in operational practice, but not found under this account or any organization it belongs to. Scope cannot include a repository whose canonical URL is unknown. | Michael |
| 11 | Public default branches for governed repositories | `dotmac_sub`, `dotmac_crm`, `dotmac_erp`, and `dotmac-integration-client` are public. ADR 0003 resolves this for `dotmac_governance` only; whether public default branches are compatible with the configuration-and-secrets process across the governed set is still undecided. | Michael |
| 12 | Self-hosted runner exposure on a public repository | `governance-checks.yml` runs on `pull_request` against the Seabone self-hosted runner. Public forks can execute code on it unless Actions requires approval for all outside contributors. ADR 0003 makes this a condition of publication; it must be verified, not assumed. | Michael |
| 13 | ~~Coordinated Dotmac ISP construction and Sub cutover programme~~ | Resolved 2026-08-20: Michael explicitly accepted ADR 0012 and authorized its acceptance amendment. Acceptance approves the two-track programme boundary but moves no production authority; every cohort remains blocked by its own evidence controls. | Michael |
| 14 | Production deployment ownership for the canonical ISP target | Michael assigned `michaelayoade/dotmac-isp`, its independent thin runtime and independent database boundary in the 2026-08-20 working session. The production deployment owner and any target host remain deliberately unassigned. | Michael |
| 15 | Enforceable legacy Sub transition rule | The intended exception classes are containment, evidence repair, migration or shadow adapters, and bounded in-place module adoption that retires one local writer without claiming target cutover. The enforcement owner, detector, exception approval and sensitivity proof must be approved before the programme relies on the rule. | Michael |
| 16 | Analytics and reporting owner for the final ISP cohort | Starter's measured family remains source-unassigned. The final cohort cannot begin by turning that measurement bucket into a package name. | Michael |
| 17 | Machine-readable representation of ADR 0013's oracle kinds | ADR 0013 defines four typed oracle kinds and their required coordinates, but adds no `standards-profile.schema.json` field and no `standards_control` rule. Whether the profile carries external claims — and therefore whether § 3 and § 4 are enforced or remain review discipline — is a separate decision gated on ADR 0013 being accepted. | Michael |
| 18 | Resolving oracle citations against their producing systems | ADR 0013 can check that a citation carries a run id, peeled commit, digest and path. It cannot check that the run did what the citation says. Closing that gap means the control plane calling GitHub and the private index, which raises access, credential, retention and rate-limit questions the current evidence model has not decided. Stated as a gap rather than closed. | Michael |
| 19 | ~~Where an authority-cutover receipt is stored, and who owns that store~~ | Resolved 2026-08-30: Michael assigned the cross-repository authority-cutover receipt registry to Governance — append-only, versioned, non-sensitive envelopes only, corrections by supersession. Products retain their local evidence and Knowledge remains discovery support, not the authority. Recorded in ADR 0018 § 3, which also decides files-in-this-repository over a service. The registry's directory, schema, parser and validator are decision 21. | Michael |
| 20 | Propagating ADR 0018 into the fleet authority-cutover record | The parent standard is `dotmac_starter_mt` ADR-0031, `Accepted` and fleet-scoped, read at commit `ed3ac864b350d4556808a69496f999f764682442`. This repository's `Amends:` field is scoped to its own ADR directory, so ADR 0018 states the relationship in prose. Whether ADR-0031 gains a matching in-document amendment — and who makes that change in a repository Governance does not own — is undecided. | Michael |
| 21 | Receipt-registry directory, envelope schema and append-only enforcement | **Engineering half delivered, authorization half still open.** ADR 0019 (`Proposed`) creates `receipts/`, the closed envelope, the strict parser and the append-only validator (`tools/check_receipts.py`, comparing an existing receipt's bytes against the merge base, never the diff's shape; known-bad controls in `tests/test_check_receipts.py`). The registry ships **empty**: authorizing the first receipt is Michael's, and no receipt may be written until he approves one. Whether the envelope is represented in `standards-profile.schema.json` also remains undecided. | Michael |

| 22 | Propagating the commit-identity gate to the rest of the governed estate | ADR 0020 (`Proposed`) adds `tools/check_commit_identity.py` to this repository's workflow only. Whether it becomes a `standards-profile.schema.json`-declared standard evaluated by `standards_control`, or is copied as a workflow step per repository, is undecided — and an enrolled repository without it is an unmonitored region, not one covered by the standard. | Michael |
| 23 | Remediating `dotmac_sub`'s existing commit identities | Observed at `d5ed100404cbcb01d500b3d8951814673708c7ee` on 2026-08-30: 1161 of 5217 reachable commits carry `noreply@anthropic.com` as author and 1143 as committer, spanning 2026-04-17 to 2026-08-29, and 4624 carry a `Co-Authored-By` trailer. This is an as-of observation under ADR 0013 § 4 and must be re-read against the canonical `main` before it is used to size any work. Rewriting a shared `main` changes every downstream hash; whether that is done, partially done, or the record is simply preserved is a decision with its own risk assessment, and ADR 0020 § 3 deliberately does not depend on it. | Michael |
| 24 | Which half of ADR 0021's dependency-floor rule is automated | **ADR 0021 is `Accepted` as of 2026-09-01 (effective the same day), and this row is what acceptance deliberately did NOT decide.** ADR 0021 has **no fleet enforcement**; `dotmac-deployment-control` is its named product-first reference implementation, which is not the same thing. **Amended 2026-09-01 with a third derived fact:** the module name the mutation lane greps for is derived from the same source of truth as the floor, not written down. Measured at that distribution's next floor bump — `>=0.1.0a98` to `>=0.1.0a100`, excluding a99, which contains the module the old literal named — so the literal demanded a failure that cannot occur, and would have gone green proving nothing had the name appeared for any unrelated reason. Repaired there in pull request #17 (`6b1ce371b07220914696243647aeb0d3947b87cc`) by `scripts/kernel_floor.py symbol`; the same pull request removed a second instance, a `0.1.0a97` literal in the implementation's own `test_the_cli_prints_the_mutation_target`. Only declaration checking is decidable from repository content; which release first shipped an imported module needs a `peeled_tag` oracle (open decision 17), and whether a lane installed the exact minimum, ran the mutation red, and excluded the publisher are facts about runs in another repository under ADR 0013 § 1. A module-name literal in a negative lane is newly decidable **in the publishing repository's own workflow file** with no oracle, which narrows what a future family could reach; it does not make it a Governance check. **Amended again 2026-09-01 with three further rules (§§ 8, 9, 10):** a lane enforces every condition it advertises rather than borrowing one from another function's invariant; a coordinate is checked to be live, exact and sensitivity-tested, since a required field validated only as a non-empty string is satisfied by `unknown`, `recently` and `assumed`; and an ASSEMBLY's pin equals the maximum floor derived from its composed distributions' `Requires-Dist`. Each is enforced by nothing, and they sit at different distances from being enforceable, which is the part this decision needs. § 8 requires a lane's source, its tests and the prose that advertises the property to be compared against one another, and is decidable only inside that lane's own repository — in the measured instance the conjunction had never been written down, so there were not two sides to compare. § 9 splits three ways: that a coordinate field is REQUIRED is decidable in the lane's repository, that it RESOLVES is an external-oracle question ADR 0013 § 5 permits only through a declared oracle this repository does not have for a run, tag, index listing or artefact path, and that the check is SENSITIVITY-TESTED is a fact about another repository's test suite under ADR 0013 § 1. § 10 is not even declaration-checkable from a checked-in tree, because the maximum is derived from INSTALLED artefact metadata — a property of a resolved environment, not of repository content — and `standards-profile.schema.json` carries no field for a pin, a composed distribution set or a floor lane. Reference implementations: `dotmac_starter_mt` pull request #572 (merged `e729ebebedb0b16f7b743bc3862e2152b64ddefc`) and #559 (merged `7a8c22df3538f3965cf48a2a5a1aa3e60fd82474`) for §§ 8 and 9; `dotmac_platform_control_plane` pull request #111 for § 10, which is **OPEN and green, not merged**, and which cites this record nowhere. **Both edges of § 10 were CLOSED by Michael Ayoade on 2026-09-01** and are now §§ 10.1 and 10.2 of the record. (i) An assembly's OWN imports DO join the maximum, and — per a SECOND ruling the same day, which closed a circularity the first left in place — the assembly's contribution is **DERIVED from its imports** using the same first-shipped provenance map as a publisher floor, rather than read from its declaration. Its declared direct constraint STATES that result; it does not ESTABLISH it. (The first form was circular: an assembly's declared direct constraint on the dependency IS the `==` pin, so a maximum taken over it returns the pin and agrees with itself.) The required order is: parse assembly imports at the exact source revision; resolve imported symbols/modules to first-shipped versions; **REFUSE unknown or ambiguous provenance**; compute `assembly_import_floor`; compute the maximum of that and the composed `Requires-Dist` floors; require the exact pin to equal it; and plant an assembly import above the current floor and require failure. Step 3 is what makes the rest honest — a map that answers for a symbol it does not know puts a fabricated number into step 4, and an unresolved symbol treated as imposing no constraint LOWERS the maximum, so the under-constrained floor this record opens with returns wearing a green lane. The plant is what converts the measured equality's premise, that nothing in the assembly out-imports its modules, from an unstated coincidence into a checked property. (ii) An assembly that publishes no distribution runs its lane IN THE ASSEMBLY REPOSITORY against its exact resolved lock or image candidate, reading installed package metadata plus its declared constraint; the measured pre-merge-plus-admission-gate shape is § 6's answer in the assembly's own terms rather than a gap — and the SECOND ruling splits that lane in two along a builder/verifier axis: source-side import and floor derivation MAY run in the candidate build job, while artefact-side adoption evidence MUST run in a separate verifier job against the immutable candidate image digest and MUST NOT rebuild the image. No registry is required. **That closes the § 6 residue this row previously carried**, and corrects its reasoning: § 6's exclusion of the publishing job was never about the registry but about a job reporting on the bytes it produced, so a builder holding the image it just made is not an independent witness of it, exactly as a publisher holding the wheel is not. **Closing all of this settles what the rule SAYS and builds nothing.** §§ 8, 9 and 10 remain enforced by nothing, here or anywhere, the new planted-import requirement included — it is decidable, if anywhere, inside the assembly's own repository against a tree and an installed environment Governance never sees — and this decision's own question, which half is automated, is untouched. The § 10 reference implementation `dotmac_platform_control_plane` pull request #111 was re-read on 2026-09-01 and is still OPEN with no merge commit. **A claim that its lane does not read the assembly's own imports is RETRACTED** — at head `5f7ebba8`, observed 2026-09-01, that branch carries `assembly-needs` and `assembly-satisfied` subcommands invoked from its `kernel-pin` job. The retracted claim was made without reading the branch, is a fact about another repository under ADR 0013 § 1, and had gone stale within hours (the head had moved from `2dd1ef7d`), which is ADR 0013 § 4's temporality point arriving inside this record. **Nothing about what the rule SAYS remains open; the automation question is untouched.** Nothing here reads a provenance map, so nothing can tell one that refuses an unknown symbol from one that answers for it, and whether a lane's artefact-side half runs in a separate verifier job against an immutable digest is a property of another repository's workflow runs under ADR 0013 § 1. Until this decision is made an enrolled repository without a floor canary is an unmonitored region, and ADR 0021 may not be cited as a gate. **Promoted to `Accepted` on 2026-09-01 on Michael Ayoade's ratification** ("promotion to Accepted is ratified. Use a human-attributed commit, preserve amendment history, and keep 'fleet enforcement: none' accurate. This does not authorize kernel a100/a101."). Acceptance changed the record's STATUS and nothing about the world: every enforcement statement in it still reads `none`, no `standards_control` rule, schema field, CI gate or check family was created, no enrolled profile changed, and **this decision is untouched and still open**. An earlier sentence in the record saying acceptance was CONDITIONED on this decision was falsified by the ratification and now states what happened instead. Acceptance also authorizes **no kernel pin move** — not `0.1.0a100`, not `0.1.0a101` — which the record now says plainly, because § Context measures a floor bump to a100 and § 10 requires a pin to equal a derived maximum, and a reader could otherwise take the two together as a standing instruction. Separately, the record's § Drift prevention reason was corrected in the same change: `check_adrs.py` and `check_adr_references.py` are this repository's only ADR-directory ENUMERATORS, not its only readers — `standards_control`, `agent_control` and `programme_control._adr_state_claims` each read individual records at declared paths — and the enforcement verdict is unchanged because none of the five evaluates a floor lane. | Michael |
| 25 | Owner and interval for the recovery-bundle rehearsal | ADR 0022 is **`Accepted`** as of 2026-08-30 (correction #46 `113b7cd`, merged `6f68ef1`); this row is what acceptance deliberately did **not** decide. The record assigns the bundle format, closure computation and rehearsal harness to `dotmac-deployment-foundation` and the rehearsal trigger to deployment control. Neither repository has accepted that assignment, and no rehearsal interval is set. The credential half is **resolved**: Michael approved the post-restore binding model on 2026-08-30 and it is recorded as ADR 0022 § 5. Acceptance creates no check family, no standards-profile surface and no CI gate, so an enrolled repository without a rehearsal is an unmonitored region rather than a covered one. Every existing fleet backup remains a data copy until this is resolved, and every recovery previously recorded `PROVED` under the schema/row-count/heads definition is reclassified **unproven** rather than failed. `PostgresRecoveryBundleV1` (Starter #518, `d6b9aae5`) is the reference implementation; it is not fleet coverage. | Michael |
| 26 | A served-directory field in `standards-profile.schema.json` | ADR 0023 (`Proposed`) amends ADR 0014 so identity covers bytes served from outside the image. The decidable half — a copy step reading the source tree into a served directory, a `--delete` sync with no empty-source guard, a served directory with no recorded digest — needs the deployment surface declaration to gain a served-directory field, which changes the schema for every enrolled repository. Whether that field is added, and who owns the migration, is undecided; the external-probe half stays review discipline under ADR 0013 § 5 regardless. | Michael |
| 27 | Enforcement, and the replay-evidence period, for the fleet integration standard | ADR 0024 (`Proposed`, renumbered from the unmerged pull request #17) states three standards Michael approved on 2026-08-15 and deliberately builds no enforcement: a `standards_control` rule family enforcing an unapproved standard would activate policy without approval, and `required` mode needs an `Accepted` source. Separately, the 180-day replay-evidence horizon is stated and explicitly **not** put in force — `integration-retention-adoption.toml` carries `replay_evidence_period = "UNSET"`, `dotmac-integration` has no second-phase retirement, DPO acceptance is `PENDING`, and receipt deletion still cascades legal-hold history. Also open: the named repair owner for Sub's digest-keyed Meta receiver. | Michael |
| 28 | Enforcement for the staged-specification standard | ADR 0025 (`Proposed`, renumbered from the unmerged pull request #18) requires three parts together: `xfail(strict=True, raises=<exact blocker>)`, an **unmarked** absence ratchet, and a harness-reachability proof. The first two are structurally detectable and could become a `standards_control` rule family with its own sabotage proofs and a schema-version increment. The third is a review judgement a syntax engine should not claim to discover. Which parts are automated, and whether the third stays a named review obligation, is undecided — and building enforcement for a `Proposed` standard would activate policy without approval. | Michael |
| 29 | A runtime-identity surface in `standards-profile.schema.json` | ADR 0026 (`Proposed`) requires five runtime-readable identity fields and a controller that REFUSES a missing or mismatched identity. Runtime `DeploymentIdentity.v1` exists (`dotmac_starter_mt` #531, `56f46f4c`) but **no product emits the labels in production**, so the fleet's current state is still the one the record describes: a controller reading fields nothing writes. The decidable half — a controller resolving identity from a tag, project name or hostname; a warn-and-continue branch on mismatch; a surface emitting fewer than five fields — needs a declared runtime surface, which changes the schema for every enrolled repository. Whether a running object actually advertises its identity is a `deployment_run` claim under ADR 0013 § 5 with no oracle. | Michael |
| 30 | Owner for repairing Workspace's direct-grant isolation tests | ADR 0022 § 3 property 9 is now **`Accepted`** and requires effective TABLE or COLUMN privileges across all seven table privileges. Workspace's isolation tests answer the same question with a catalogue listing, which sees **direct grants only** — so a role reaching a table through a membership reads as having no access and the assertion goes green over the leak. Recorded and **unowned**. Until an owner repairs them, those tests must not be read as evidence of plane isolation, and the risk is that a reader treats a passing suite as conformance with an accepted standard it contradicts. | Michael |
| 31 | Propagating ADR 0027 into `dotmac_starter_mt`'s own records | ADR 0027 (`Proposed`) narrows where a module may be PROVED without re-deciding anything in `dotmac_starter_mt` ADR-0057 or `AGENTS.md` hard rule 24, both cited rather than restated. This repository's `Amends:` field is scoped to its own ADR directory, so the relationship is stated in prose — the same situation as decision 20. Whether Starter's records gain a matching statement, and who makes that change in a repository Governance does not own, is undecided. Note also that § 3's dossier requirement is a declaration check at best: "a production path actually reads and writes it" is not decidable from repository content, so this record is stated review discipline with no guard. | Michael |
| 32 | Enforcement for the 2026-08-31 standards wave (ADRs 0028, 0029, 0030, 0031) | **A decided position, and ADR 0031's half is now BUILT.** For 0028–0030 no engine control exists and the reason is shared: a checker over the PRESENCE of steps passes the systems these records were written from. ADR 0028's trap is exact — a repair step and a verify step are both present in the broken deploy path — and the same shape defeats a naive 0029 check (a profile that declares itself correctly is indistinguishable from one that lies) and a naive 0030 check (a policy module exists whether or not any assembly applies it). **The decidable form gates on a TYPED RECEIPT and its contents, not on the presence of steps**, which places that work AFTER the receipt chain exists. Condition for revisiting: a typed receipt whose contents a gate can read. **ADR 0031 was the separable one and is done:** `tools/check_adr_references.py` with known-bad controls in `tests/test_check_adr_references.py`, wired into this repository's workflow and validation contract. It is **scoped to this repository only** — see decision 39 for propagation, which is blocked on `dotmac_platform_control_plane`'s own citation sweep. | Michael |
| 33 | Owner and conforming form for `dotmac_sub`'s module-schema prerequisite path | **Moving as of 2026-08-31.** Sub's provisioning is staged and its prerequisite gate has been run in **both** environments, with production matching the contract exactly. It is stopped on a **credential-minting grant with Michael** — the elevated connection ADR 0028 Form A requires the managed path to own — not on an unowned design question. What remains to record once that grant lands: which form the path formally adopts, and the typed receipt covering both the repair and the verification halves. The earlier state of this row (three failed production repair runs, `mod_inbox` unprovisioned) is superseded. | Michael |
| 34 | ~~Propagating ADR 0028 into `dotmac_starter_mt` ADR-0018~~ | **Routed 2026-08-31 — off this list.** ADR-0018's dated addition in `dotmac_starter_mt` goes to the executor-retirement lane, carrying that lane's own guard-checks-a-name-instead-of-a-property evidence: a `"PYTHONPATH" not in dockerfile` check failing on the comment explaining its absence, a ledger test catching its own docstring, an executor verb detector reading a usage comment as a deployment, and a connector ratchet failing on a type-annotation import. That evidence set is a better fit for the amendment than ADR 0028's deployment case, and splitting it would have produced two partial additions to one record. | Michael |
| 35 | Measuring the other profile-selecting assemblies against ADR 0029 | **Approved 2026-08-31, low priority, read-only, and blocking nothing.** `dotmac_starter_mt`, `dotmac_sub` and `dotmac_erp` are each audited for a production-accepted profile publishing a simulation-only surface, a permissive unset-profile fallback, and hand-enumerated guards under ADR 0029 § 5. Worth knowing rather than urgent: no production exposure is suspected, and the measurement is observation only — it changes no code and opens no gate. Queued behind ADR 0031's citation repair. The enforcement half remains decision 32. | Michael |
| 36 | Measuring the other profile-selecting assemblies, and propagating ADR 0029 into product records | ADR 0029 binds `dotmac_starter_mt`, `dotmac_sub` and `dotmac_erp` as much as Platform CP, but only Platform CP has been measured (PR #93, `8a2ac5a2`, and its ADR-0015). Whether the other three are audited for a production-accepted profile publishing a simulation-only surface, for a permissive unset-profile fallback, and for hand-enumerated guards under § 5 — and who does it — is undecided. Separately, this repository's `Amends:` field is scoped to its own ADR directory, so the relationships to `dotmac_starter_mt` ADR-0018 and Platform CP ADR-0015 are stated in prose; whether those records gain matching statements, in repositories Governance does not own, is the same situation as decisions 20, 31 and 34. | Michael |
| 37 | The undischarged kernel obligation behind ADR 0030 | **Owned and UNBLOCKED as of 2026-08-31.** Starter #540 is merged (squashed `87e5e005c50f83eec51b4985a763206d3743fc78`), so the `gh pr merge` permission that was holding this is cleared and the kernel API-documentation port is next in the Starter release integrator's queue, per `dotmac_platform_control_plane` ADR-0016 § 6 with `src/vendor_cp/api_documentation.py` as the ADR 0006 product-first implementation to PORT. **The separate open fact stays open:** the construction-time refusal — an ABSENT declaration refusing the build, rather than a nullable field resolving to the framework default — is specified in that § 6 and implemented nowhere, so ADR 0030 § 2 remains a requirement no fleet assembly meets. The port must FIX that rather than inherit it; a port that reproduces the consumer-side shape would carry the gap into the kernel where every assembly gets it. Until it lands, `dotmac_starter_mt`, `dotmac_sub`, `dotmac_erp` and `dotmac_workspace` each still mount `/docs`, `/docs/oauth2-redirect`, `/redoc` and `/openapi.json`. | Michael |
| 38 | ~~Whether cross-repository ADR references must be repository-qualified~~ | **Resolved 2026-08-31: yes.** Michael ratified the convention and it is recorded as ADR 0031 — a cross-repository ADR reference names its owning repository, and a bare number refers only to a record in the citing repository. The second half is the load-bearing one: it makes Platform CP's seven bare `ADR-0018` references (all meaning `dotmac_starter_mt`'s) defective **today**, while they still resolve by elimination, rather than on the day that repository writes its own 0018 and all seven silently re-point with no edit. The exhibit is inside the exemplar: `dotmac_platform_control_plane` `docs/adr/0016-api-documentation-exposure-policy.md:11-12` cites "ADR-0018 in `dotmac_governance`" for a rule that is `dotmac_starter_mt`'s — the one place its author stopped to qualify a cross-repository reference, and it still went wrong. No ADR number changes anywhere; no sweep is mandated. Whether the local detector is built is decision 32. | Michael |
| 39 | Propagating the ADR 0031 citation guard beyond this repository | **Blocked deliberately, and the reason is a measured red gate.** `tools/check_adr_references.py` runs on `dotmac_governance` only. Run read-only against `dotmac_platform_control_plane` at `8a2ac5a2` it reports **43** unresolvable citations across five numbers — ADR-0031 (13), ADR-0028 (12), ADR-0018 (9), ADR-0023 (6), ADR-0024 (3) — every one meaning a `dotmac_starter_mt` record. **All five numbers now also exist in `dotmac_governance`**, and 25 of the 43 became ambiguous on 2026-08-31 when ADRs 0028 and 0031 merged here: the wave that states the rule widened the ambiguity it describes. Enrolling the guard before that repository's sweep lands would turn it red on 43 pre-existing findings, which is precisely the surprise this wave has avoided four times. Sequence: the sweep lands there, behind its two active lanes; then propagation is decided. Note the earlier figure of "seven" was a `.py`/`.md`-only grep for ADR-0018 alone and is superseded by 43. | Michael |
| 40 | Enforcement for the descriptor-authority standard, and the artefact that gates it | **`Accepted` as ADR 0032 on 2026-08-31, unenforced, and the obstacle is NAMED rather than undecided.** Unlike decisions 32's records, this one is not blocked on a design nobody has done: the vocabulary that would express a declared database state is reported to be `DatabaseContract` in `dotmac-deployment-foundation` `0.3.0a2`, which is **deliberately held** — `dotmac_platform_control_plane` `docs/adr/0017-declarations-inventory-and-the-artifact-that-validated-them.md:131` states "publishing before rehearsal would recreate the deadlock the candidate lane exists to break", and § 6 records that the pinned candidate artifact **expires 2026-11-28**. So this can be ratified and specified now and enforced when 0.3 publishes. **The expiry is NOT yet a forcing function and must not be cited as one:** the pin exists only in that ADR's prose — the file § 6 names was deleted five minutes before the record landed, the run/artifact/version/date coordinates appear nowhere in code, and `pyproject.toml` states deliberately that the Foundation is not a dependency of that assembly. Making the pin real is a prerequisite to relying on the date. Four decidable shapes and the non-vacuity case are enumerated in ADR 0032 § Drift prevention, including the one that matters: a comparison must be shown reporting **present-but-undeclared**, because the measured instance is entirely of that kind and a checker exercised only on declared-but-absent would have passed it. Note the type name and contents are attributed, not verified — Foundation is not checked out locally. | Michael |
| 41 | Enforcement for the absence-evidence standard | **`Accepted` as ADR 0033 on 2026-09-01, and enforced by nothing.** The record amends ADR 0013 § 4 on one point: § 4 governs a negative claim's FORM — as-of observation, coordinates, named refresh owner — and says nothing about the INSTRUMENT, so a claim can satisfy it perfectly and be false at the moment it was taken. The measurement is a `dotmac-ticketing` consumer census in which `gh search code` returned zero hits for a string present in hundreds of files in a public repository; the lane caught it only because it positive-controlled the query against known consumers first, and it then refused the tool rather than downgrading the claim. **The generic detector is already refused and this decision does not reopen it:** ADR 0013 § 5 rules out a prose scanner, which cannot separate a claim from a description of one and would flag ADR 0033's own recital. What is decidable is narrower and lives elsewhere — where a census is a TOOL rather than a paragraph, three properties are decidable from that tool's own source and tests in its OWN repository: a positive control that runs before the claim on the same instrument and credential, an enumeration built from a declared inventory rather than from the search's own results, and an incomplete-enumeration path that REFUSES rather than returning a subset. Whether that family is built, by whom, and whether `standards-profile.schema.json` gains a census surface is undecided; Governance cannot observe another repository's test suite under ADR 0013 § 1. Until this is decided an enrolled repository whose absence claims rest on a code search is an unmonitored region, and ADR 0033 may not be cited as a gate. | Michael |
| 42 | Sequencing and schema details for the real-target-admit standard | **`Accepted` as ADR 0034 on 2026-09-01, enforced by nothing, and the SEQUENCE was settled by Michael Ayoade the same day — so what is open here is now narrower than the question this row first asked.** Three phases, in order, and the order is the fleet's product-first extraction rule rather than a scheduling preference: (1) **`dotmac_sub` supplies the first real-target demonstration**, after its bootstrap creates an admissible target; (2) **Governance extracts the typed family afterward**; (3) **the standards profile gains its surface only after executed product evidence exists**. Until then **ADR 0034 remains normative with enforcement absent** — a phased plan is not a control, and the record must keep saying so. Governance writing the typed family ahead of a working implementation would be ADR 0034's own defect one level up: a contract authored against no real subject, whose fixtures could only be built from the contract. **What remains open is the schema details**, gated on executed product evidence that does not yet exist; whether the family is built, by whom, and whether the profile gains a surface are DECIDED. A gate that ENUMERATES its targets must be shown admitting one of them, read from the artefact production actually uses, through the entry point a dispatch reaches. The measurement is `dotmac_sub`'s v2 published-port PLAN gate at `origin/main` `c40a914d99adb69e67bcc90b5e14881c18348268`: its dispatch offers exactly `postgres-local` and `freeradius`, its contract requires `^[^\s@]+@sha256:[0-9a-f]{64}$`, and the production compose file pins **no** image by digest — so every possible dispatch refuses inside the observer, twice over (the target's own tag, and any of six tag-pinned non-target containers the snapshot contract also covers). The suite had synthetic acceptance and planted refusals and neither can see it: a synthetic fixture is built to pass, a planted violation is built to fail, and the missing case is a real subject from the declared set passing. This is the **mirror** of `dotmac_starter_mt` ADR-0018 rule 5 and of ADR 0021 § 9's sensitivity proof — those observe a guard FAILING, this observes it SUCCEEDING on a real target; a guard observed only failing is indistinguishable from one that refuses everything. Also recorded: the second-order deadlock, where APPLY is the only managed path to the state PLAN requires, so the gate can make its own repair unreachable. **What is undecided is enforcement, and Governance cannot hold it.** Deciding the property needs the gate's enumeration, the production artefact and the gate's test suite at once; all three live in the gate's own repository, and whether another repository's suite demonstrates an admit is a fact about that suite under ADR 0013 § 1. Three properties are decidable there — every enumerated member appears as an ADMIT subject; the subject is READ from a named production path rather than transcribed; the assertion is on the dispatch's entry point. The three properties decidable in the gate's own repository are unchanged, and Governance still cannot observe them under ADR 0013 § 1. Note also that `dotmac_sub`'s in-flight repair (`feat/legacy-image-pin-bootstrap` `25c5bbca3`, **unmerged**, observed 2026-09-01, an as-of observation under ADR 0013 § 4) narrows the rule to the target and adds a legacy tag-to-digest bootstrap, and still adds no real-subject admit — so it breaks the deadlock without discharging this standard. Until this is decided an enrolled repository holding an enumerating gate that has never demonstrated a real-target admit is an unmonitored region, and ADR 0034 may not be cited as a gate. | Michael |
| 43 | Whether a record's status can be checked against its own change's stated intent | **Found 2026-09-01, unowned, and NOT fixed by the ADR 0016 ratification amendment.** `tools/check_adrs.py` validates a record's controlled metadata against itself and against the other records. Nothing compares that metadata against what the CHANGE carrying it says about itself, so a file reading `Status: Accepted` merges inside a pull request titled "PROPOSED, not normative" with every check green. **Measured twice, not once.** ADR 0016: PR #37 added the record as `Proposed` (`2fd8231a`, 05:14:26Z), set it `Accepted` with `Effective: 2026-08-30` (`031b3895`, 05:53:53Z) and merged four minutes later, title and body never updated; squash commit `00a27bab` carries the stale title as its subject and the acceptance in its body. ADR 0013: PR #22, titled "Propose the repository-local claims and external oracles standard (ADR 0013)", carried a propose commit and an accept commit authored in the same minute and merged as `Accepted`. In both the framing was accurate when written and stale when merged, which is the load-bearing detail: **the defect is not a wrong claim, it is an unrefreshed one**, and a guard aimed at wrongness would not have fired on either. **Decidability is the open question, not the priority.** A pull request title is prose; a stale title is indistinguishable by inspection from one that was wrong from the start; and the narrow shape that is decidable -- a diff moving a `- Status:` line to `Accepted` while the change's own subject asserts the record is not normative -- is neither the whole class nor obviously worth its false positives, since it fires on exactly the legitimate two-commit sequence both instances used. **Recommendation, to be decided rather than assumed:** prefer a PROCESS control to a prose scanner -- require that the change which sets a record `Accepted` state that status in its own subject, and treat the pull request title as an unreliable surface rather than a checkable one; a squash subject is derived from the title and is the artefact that persists. If a guard is built, it must carry a sensitivity proof showing it red on the two measured instances and green on a promotion whose subject and record agree. Until this is decided, a record whose merge framing contradicts its status is an **unmonitored region**, not a covered one. | Michael |
| 44 | Enforcement, dossier-schema ownership and the digest surface for the foundation profile | ADR 0039 (`Proposed`) states the `ApplicationFoundationProfile.v1` contract and deliberately builds no enforcement: a `standards_control` rule family enforcing an unapproved standard would activate policy without approval. Three parts are open and they sit at different distances from being decidable. **(a)** Profile completeness and the closed binding field set are decidable from repository content wherever a profile is checked in, and would need a declared surface in `standards-profile.schema.json` — which changes the schema for every enrolled repository, as in decisions 26 and 29. **(b)** Installed-artifact resolution (§ 5), the positive/negative admission tests (§ 7) and the post-deployment read-back (§ 8) are facts about another repository's workflow runs, an image digest and a running system; ADR 0013 § 1 places them outside repository-local claims and § 5 admits them only through a declared oracle this repository does not have. That is decision 17. **(c)** § 9 requires a retirement DISPOSITION as typed data — the fourth property of a shape whose first three already exist as `[[product_writers]]` (`writer_state`, `retirement_required`, `evidence_paths`) in `dotmac_starter_mt`'s `EXTRACTION.toml`, which is the fleet's current instance of the shape rather than the requirement itself. Adding it is a change in a repository Governance does not own — the same situation as decisions 20 and 31 — and who owns it is undecided. Measured 2026-09-03 at `dotmac_starter_mt` `origin/main` `d096e64c13fe3cd8ab89f4a15edd1ce1bc046e2a`: 340 `[[product_writers]]` rows carry exactly five keys and none of them records that a retirement happened, so 140 owed retirements are countable and completed ones are not. That repository has already ruled the prose alternative out (`a sentence is not a claim a checker can compare`, with `test_the_expenses_failure_would_now_be_refused` as the measured failure), so the gap is a missing typed field rather than a missing convention. Until it is repaired the programme metric — local writers retired — counts sentences rather than facts. Also undecided: whether the profile digest appears in this repository's `receipts/` envelope, which ADR 0019 § 2 closed deliberately and which ADR 0039 does not touch. Until this decision is made an enrolled repository without a foundation profile is an unmonitored region, and ADR 0039 may not be cited as a gate. | Michael |
| 45 | Implementation owner, and the platform-trail edge, for the anonymous audit actor | ADR 0040 (`Proposed`) adds `anonymous` as the fifth Audit v2 actor kind — `actor_id` absent, `actor_label` absent, Party enrichment forbidden STRUCTURALLY — and builds nothing. Both seams it requires are properties of code and a schema in `dotmac_starter_mt`, which Governance does not own: the same situation as decisions 20, 31 and 44. Measured 2026-09-03 at that repository's `origin/main` `d096e64c13fe3cd8ab89f4a15edd1ce1bc046e2a`: `resolve_audit_actor` takes `actor_party_id` and never reads it, and `AuditEvent` declares no `__table_args__`, so the four documented per-kind party rules are enforced by nothing today — which is why the record requires a shape rather than a fifth comment line. Open: **(a)** who implements the construction seam, the persistence constraint and their two required-to-fail negative controls plus the admit control; **(b)** whether the PLATFORM audit trail's actor gap is closed the same way — `write_platform_audit_event` identifies its actor as a nullable `actor_admin_id` with no kind column, so an unauthenticated platform login attempt is indistinguishable from an unrecorded actor, and ADR 0040 § 6 deliberately does not decide it; **(c)** whether the two existing unenforced per-kind rules (`system` and `service` carry no party) are repaired in the same change, which needs a count of existing violating rows that only the owner can take. Whether the required controls exist is a fact about another repository's tests under ADR 0013 § 1. Until this is decided, no product may be assumed to refuse an anonymous row carrying a party, and ADR 0040 may not be cited as a gate. | Michael |

Decisions 1, 2, and 3 are struck through pending ADR 0002 approval. While that
record is `Proposed` they remain open, because a proposed record closes nothing.

## Directed bootstrap decisions

Michael has already directed:

- `dotmac_governance` owns organization-wide policies, control definitions,
  global ADRs, templates, and generated indexes. It was private at bootstrap;
  Michael directed on 2026-07-26 that it become public so branch protection is
  technically enforceable. ADR 0003 records that decision, narrows hard rule 5
  from visibility to classification, and makes an Actions fork-approval policy
  a condition of publication.
- Initial standards scope is ISO/IEC 27001 and ISO/IEC 42001, with
  ISO/IEC/IEEE 12207:2026 and ISO/IEC/IEEE 15289:2019 as engineering
  references. ADR 0002 proposes inverting this — 12207 and 15289 as the spine,
  27001 and 42001 as overlays — and is not yet approved.
- The product is a standards-based development model, not a certification
  programme. ADR 0002 records this; until it is approved the destination stated
  in ADR 0001 stands.
- ISO 9001 certification is deferred until a customer, procurement, or
  company-wide QMS requirement exists.
- Interim accountable approval roles are recorded in ADR 0001. The surrounding
  ADR was explicitly accepted by Michael on 2026-07-25 and becomes effective
  when its accepted revision merges to `main`.

## Resolved operational control: private CI

The repository had been made public as a workaround for unavailable hosted
Actions. That conflicted with the directed private boundary and was corrected on
2026-07-24. The replacement control is a repository-scoped Seabone
self-hosted runner.

Private-repository CI was verified green in GitHub Actions run
[`30128057161`](https://github.com/michaelayoade/dotmac_governance/actions/runs/30128057161).
The run selected the Seabone runner, exercised the known-good and known-bad ADR
controls, and validated the production record set. This closes CI availability;
it does not close open decision 7 because the current plan still cannot require
that check before merge.
